import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#FCA311",
      light: "#fdb541",
      dark: "#e3930f",
    },
    secondary: {
      main: "#1a1a1a",
      light: "#333333",
      dark: "#000000",
    },
  },
});
